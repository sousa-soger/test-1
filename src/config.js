const config = {
  apiUrl: "https://api.example.com",
  environment: "production",
  enableNewDashboard: true,
  enableLogin: true,
  enableInvoice: true,
  enablePackage: true
};

module.exports = config;